# Oil Spill Detection & Vessel Attribution Frontend

React + Vite dashboard for the SIH 2026 MVP.

Run:
```bash
npm install
npm run dev
```

The dashboard calls the FastAPI `/analyze-spill` endpoint when available and falls back to demo analysis data when the backend is not running.
